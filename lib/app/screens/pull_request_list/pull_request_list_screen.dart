import 'package:flutter/material.dart';

class PullRequestListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pull Request List (PRs)'),
        centerTitle: true,
      ),
    );
  }
}
