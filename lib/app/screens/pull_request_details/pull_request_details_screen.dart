import 'package:flutter/material.dart';

class PullRequestDetailsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pull Request Details (PRs)'),
        centerTitle: true,
      ),
    );
  }
}
